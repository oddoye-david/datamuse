'use strict';

describe('#Word options', () => {
  it('Should throw exception if no options');

  it('Should throw exception if invalid option(s)');

  it('Should throw exception if option value isn\'t a string');
});

